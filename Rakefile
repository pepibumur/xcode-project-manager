task :build do 
    sh "xcodebuild -workspace XcodePM.xcworkspace -scheme XcodePM -config Debug clean build"
end

task :test do
    sh "xcodebuild -workspace XcodePM.xcworkspace -scheme XcodePM -config Debug clean test"
end
